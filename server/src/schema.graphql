type Query {
  info: String!
  feed: [Link, User]!
}

type Link {
  id: ID!
  description: String!
  url: String
}

type User {
  id: ID!
  name: String!
  age: Int!
}

type Mutation {
  post(url: String!, description: String!): Link!
}